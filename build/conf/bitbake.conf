import os
TMPDIR = "/dev/shm/%s/%s" % (os.environ['CI_PROJECT_PATH'],os.environ['CI_BUILD_REF_NAME'])
CACHE   = "${TMPDIR}/cache"
STAMP   = "${TMPDIR}/stamps"
T       = "${TMPDIR}/work"
B       = "${TMPDIR}"